# Screenshots

Drop captures here and they will render in the project README:

| File | Screen |
|---|---|
| `run-viewer.png` | Run Viewer — the semantic trace, timeline, and evaluation panes |
| `decision-lab.png` | DecisionScope Lab — perturbation result matrix and drift |
| `evaluations.png` | Evaluations — threshold explorer over a labelled dataset |
| `dashboard.png` | Dashboard — totals, latency, and interesting events |

Capture these with a `TYPESAFE_API_KEY` configured, so the screenshots show
measured Jev results rather than mock values.
